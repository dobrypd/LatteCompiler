int main()
{
    int[] a = new int[1000];
    int x = 123;
    a[0] = 1;
    int i = 1;
    while( i < a.length) {
        a[i] = a[i-1] + 1;//(a[i-1] + 2) % 10000;
        i++;
    }
    for(int x : a) {
        printInt(x);
    }
    return 0;
}
